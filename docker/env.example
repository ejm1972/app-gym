# =============================================================
# ConInf AppGym — Variables de entorno
# Copiar como .env y completar con los valores reales
# NUNCA committear este archivo (está en .gitignore)
# =============================================================

# Base de datos
DB_HOST=db
DB_PORT=3306
DB_NAME=app_gym
DB_USER=appgym
DB_PASS=appgym123
DB_ROOT_PASS=root
